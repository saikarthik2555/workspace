package com.Map;

/*
  int index=key.hashCode()%HashTable;
  or 
  int index=key.hashCode()%table.length();
  
  if(table[index]==null){
  Node n=new Node(key,value,next);			//next--> next node address
  table[index]=n;
  }
  else
  {
  LinkedList;
  }
  
 */
