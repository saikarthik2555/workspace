package com.string_buffer;

public class Program1 {
	//StringBuffer are mutable
	//but performance issue because all the methods of String Buffer are synchronizable
	//it means only one thread can executes the method so it is thread safe

}
